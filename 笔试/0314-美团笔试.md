# 0314美团笔试

## 题1

小美很喜欢因子数量为奇数的数。
小芳给了小美一个区间 $[l, r]$ ，请帮小美算出区间内有多少个因子数量为奇数的数。

**名词解释：**

因子：对于正整数 `x`，如果存在正整数 `p`使得`x`能被 `p` 整除，则称`p`是 `x` 的因子。

**例子：**

12 的因子有 1, 2, 3, 4, 6, 12。  输入格式  

第一行两个整数` l, r`，满足 $(1 \le l \le r \le 10^9)$。  

**输出格式：**

输出一个整数，表示区间 $[l, r]$ 内因子个数为奇数的数的个数。

样例（假设）  

**输入：**

1 10

**输出：**

3

**解释：**1, 4, 9 的因子个数为奇数，分别是 1 个、3 个、3 个因子。





## 题2

定义超级斐波那契数列如下：给定整数 $$k$$，该序列的前 $$k$$ 项均为 1；对于 $$n > k$$，第 $$n$$ 项为前 $$k$$ 项之和，即

$$
S_n = S_{n-1} + S_{n-2} + \cdots + S_{n-k}
$$

现给定整数 $$k$$ 和查询次数 $$q$$，每次查询一个正整数 $$x$$，请输出该序列的第 $$x$$ 项对 $$10^9 + 7$$ 取模后的值。

**输入描述**

第一行输入两个整数 $$k, q$$（$$1 \leq k \leq 10^6;\; 1 \leq q \leq 2 \times 10^5$$）

此后 $$q$$ 行，每行输入一个正整数 $$x$$（$$1 \leq x \leq 10^6$$）。

**输出描述**

输出 $$q$$ 行，每行输出一个整数，表示对应查询的答案对 $$10^9 + 7$$ 取模后的值。





## 题3

给你一个由 $n$ 个编号为 $1 \sim n$ 的节点以及 $m$ 条编号为 $1 \sim m$ 的边组成的无向图。我们定义一个节点的权值为它的当前度（即已执行完之前所有操作后的状态）加上它的节点编号。

小美会进行 $q$ 次如下操作：

- **操作一**：断开编号为 $x$ 的边，保证每条边至多被删除一次，即在进行操作一时，该边当前一定存在于图中。
- **操作二**：向你询问编号为 $x$ 的节点所在的连通块中所有节点中最大的权值，你需要将此权值告诉他。

**名词解释**

**度**[1]：与一个顶点相连的边的条数称为该顶点的度。

**连通块**[2]：也称连通分量，满足：
- 是原图的一个子图；
- 连通块内的任意两个顶点之间都存在路径相连，且路径上的点也在连通块内；
- 是极大的，即不能再通过添加原图中的其他顶点而依旧保持连通性；

单独的点也构成一个连通块。连通块的大小即为连通块中顶点的数量。

**输入格式**

第一行包含三个整数 $n, m, q$，分别表示节点数、边数和操作次数。

接下来 $m$ 行，每行两个整数 $u, v$（$1 \leq u, v \leq n$），表示一条连接节点 $u$ 和 $v$ 的无向边，边的编号按输入顺序从 $1$ 到 $m$。

接下来 $q$ 行，每行首先输入一个整数 $op$ 表示操作类型：
- 若 $op = 1$，则接着输入一个整数 $x$（$1 \leq x \leq m$），表示删除编号为 $x$ 的边。
- 若 $op = 2$，则接着输入一个整数 $x$（$1 \leq x \leq n$），表示查询节点 $x$ 所在连通块中所有节点的最大权值。

**输出格式**

对于每个操作二，输出一行一个整数，表示对应查询的答案。

**数据范围**

- $1 \leq n, m, q \leq 2 \times 10^5$
- 保证每条边至多被删除一次，删除时该边一定存在于图中。





# AI面试

**Q：**

你要提供一个文本生成HTTP接口给业务方调用，钱给你设计请求与返回的关键字段，至少包含：输入、上下文、模型参数、输出结构、错误码，以及用于追从的一次调用ID，你会如何支持流式返回？



**Q：**

你要做“成本控制”，把每次调用成本拆到业务维度，后端如何统计token用量、工具调用次数、重试次数，并按业务方、场景、模型版本做聚合报表？



**Q：**

关于表设计，你认为在这个场景中需要考虑哪些技术知识点或框架来实现高效的数据存储和查询？

在解决问题的过程中，你会如何处理不同业务方、场景和模型版本的数据聚合呢？

请描述一个你成功将一个想法转化为实际行动的例子，你是如何规划和执行的？遇到了哪些障碍？如何克服的？



