## CSZZ杀伤网评估系统

### 并发模型与计算一致性

你提到“规划线程池并行蒙特卡洛”。请问你的线程池 `corePoolSize`、`maximumPoolSize` 以及 `workQueue` 的大小是如何根据线上生产服务器的 CPU 核数确定的？在万级、甚至十万级节点规模的复杂网络下，如果单次试验（Trial）涉及深度的图搜索或路径计算，导致计算时间极度不均，你是如何进行任务拆分Task Splitting的？如何避免因某些“长尾任务”导致整个接口返回超时？

#### 1. 线程池参数怎么按 CPU 核数定

Trial 本质是图搜索/路径计算，偏 CPU 密集型，所以我用这个原则：

- `corePoolSize = Ncpu` 或 `Ncpu - 1`（给系统留 1 个核）
- `maximumPoolSize = corePoolSize`（CPU 密集场景不靠加线程提速，避免上下文切换）
- `workQueue`，用有界队列，不用无界队列
  - 常用经验：`2*Ncpu ~ 8*Ncpu`。项目当前是 `core=max=availableProcessors()-1`、`queue=128`，这是一个保守可用的初始值；线上我会改成可配置并按压测校准

```java
    @Bean("evaluationSimulationExecutor")
    public Executor evaluationSimulationExecutor() {
        // 核心/最大线程数：CPU核心数 - 1（至少为1）
        int cores = Math.max(1, Runtime.getRuntime().availableProcessors() - 1);
        ThreadPoolTaskExecutor executor = new ThreadPoolTaskExecutor();
        executor.setCorePoolSize(cores);
        executor.setMaxPoolSize(cores);
        // 队列：有界队列（128个任务）
        // 拒绝策略：CallerRunsPolicy - 队列满时，谁提交任务谁执行（通常是主线程）
        executor.setQueueCapacity(128);
        executor.setThreadNamePrefix("eval-sim-");
        executor.setRejectedExecutionHandler(new ThreadPoolExecutor.CallerRunsPolicy());
        // 关闭时等待任务完成
        executor.setWaitForTasksToCompleteOnShutdown(true);
        executor.setAwaitTerminationSeconds(60);
        executor.initialize();
        return executor;
    }
```

#### 2. 任务拆分怎么做（应对 Trial 耗时极不均）

如果是“1 Trial = 1任务”静态提交，会有长尾。
下一步，我会用 动态分片（chunking）+ 拉取式调度：

- 不预先把大块任务固定给某个线程
- 每个 worker 从原子计数器拿下一批 Trial（比如 8/16/32 个）
- 批次大小自适应，目标是单批耗时约 `50~200ms`
- 快线程多干，慢线程少干，减少最后阶段“只剩几个超慢任务”导致的拖尾

这比静态均分更适合“图规模和搜索深度波动很大”的场景。

#### 3. 如何避免接口因长尾超时

我会做三层超时保护（这点面试官很看重）：

- 请求级 deadline：例如接口最多 3s 或 5s
- 任务级超时：单个 chunk/Trial 超时直接取消或降级
- 聚合级容错：到 deadline 就停止等待，返回“已完成样本的统计结果 + completedTrials + 置信区间/误差范围”

也就是说，不是“必须等 100% Trial 完成”，而是“在 SLA 内尽可能多完成，并给可解释的统计精度”。

#### 4. 十万级节点时的额外工程化措施

- 只把并行放在外层 Trial，避免内外双层并行抢 CPU
- 图结构做只读预处理（邻接索引、启发式缓存）并复用，少用深层 Java 对象引用链（GC 压力大），改为数组/压缩邻接表（CSR 思路）
- 线程池与 Web 线程池隔离（舱壁），防止仿真拖垮接口线程，超负载时自动降级：减少 `simulateCount`、降低时间精度、返回近似值
- 限流 + 熔断：高峰时降低 `simulateCount` 或切换近似模式



### 深拷贝的代价与 GC 风险

为了隔离试验状态，你选择了“深拷贝”。对于包含大量节点、链路关系的内存对象模型，频繁深拷贝是非常昂贵的。

- 你具体采用了哪种实现方案（序列化/反序列化、手动 `Clone`、还是 `Copy-on-Write`）？
- 在高并发进行上千次蒙特卡洛迭代时，这种方案带来的 **JVM 堆内存（Heap）压力**和 **GC 频率（尤其是 Old Gen 的 Full GC）**你是如何监控和优化的？为什么不考虑在每轮试验结束后通过“状态回滚（Rollback）”来复用对象？



#### 1. 深拷贝方案

- 当前实现是 Jackson 序列化/反序列化 深拷贝，不是手写 `Clone`，也不是 `Copy-on-Write`。
- 代码上用 `ObjectMapper.writeValueAsBytes` + `readValue` 复制 `SituationVO`，每个 Trial 拿独立副本，避免线程间状态污染。
- 这么选的原因是：实现快、正确性高、对复杂嵌套对象稳妥，先保证并行结果可信。

#### 2. Heap/GC 怎么监控

- JVM 侧：开 GC 日志（`-Xlog:gc*`），重点看 Young GC 次数、Old 区占用、Full GC 次数与停顿时长。
- 应用侧：埋点统计 `deepCloneSituation` 耗时、单 Trial 耗时分位数、并发任务数、队列长度、请求总耗时。
- 剖析侧：用 JFR/Arthas/VisualVM 看对象分配热点，确认是否 clone 和快照对象是主要分配源。

#### 3. Heap/GC 怎么优化

- 控制并发度：线程池按 CPU 核数配置，队列有界，防止瞬时创建过多副本。
- 控制任务提交方式：避免一次性提交海量 Trial，改动态分批提交，降低分配洪峰。
- 降低拷贝体积：把不变拓扑抽成共享只读结构，Trial 只复制可变状态。
- 降低对象数量：减少中间临时对象，结果聚合尽量做局部汇总而不是保留所有明细对象。

#### 4. 为什么不做每轮 Rollback 复用对象

- Rollback 需要“所有状态变更都可逆”，包括节点状态、边状态、快照、缓存、副作用字段，漏一处就会污染下一轮。
- 仿真逻辑复杂时，维护 undo log 的正确性成本很高，测试成本也很高。
- 多线程下复用对象还要处理同步与可见性，复杂度进一步上升。
- 所以我的策略是：先深拷贝保证隔离正确性，再逐步演进到“不可变基座 + delta 覆盖”，而不是直接上全量回滚机制。



### 结果合并与原子性保证

蒙特卡洛需要统计大量随机试验的均值或分布。当多个线程并行计算完各自的批次后，你是如何将结果合并（Merge/Aggregate）的？在合并过程中，如何保证统计数据（如韧性指标、抗毁度）的原子性？你是否遇到了并发竞态问题，是如何解决的？

#### 1. 并行计算与合并的整体模式

- 我采用的是“并行计算，串行合并”模式。
- 每个线程只负责跑自己的 Trial，产出独立 `SimulationResult`；不在多线程阶段共享写同一个统计对象。
- 所有任务完成后，再统一进入聚合函数做均值计算。

#### 2. 具体合并流程（当前实现）

- 并行阶段：`CompletableFuture.supplyAsync(..., executor)` 提交多个 Trial。
- 同步点：`CompletableFuture.allOf(...).join()` 等所有任务结束。
- 收集结果：把每个 future 的结果 `join()` 成 `List<SimulationResult>`。
- 聚合阶段：调用 `calculateAverageResult(allResults, executionCount)` 计算指标均值和快照均值。
- 这意味着“韧性指标、抗毁度”等统计是在单线程完成的。

#### 3. 原子性是如何保证的

- 因为没有多个线程同时更新同一份累计器，所以不需要在聚合里加 `synchronized`/CAS。
- 原子性保证来自两个设计：
  - 状态隔离：每个 Trial 用独立副本运行，避免共享状态写冲突。
  - 串行聚合：最终统计在一个线程内完成，天然具备一致性。

### Redis 写入细节与数据爆炸

你提到“评估结果拆分 Key 写入 Redis”。

- **Key 的粒度是什么？** 如果一个想定（Scenario）包含 10,000 次 Trial，你是存 1 个大 JSON，还是 10,000 个小 Key？如果是前者，Redis 的 `BigKey` 问题怎么解？如果是后者，如何保证“结果写入”的原子性？（万一写到一半应用挂了，Redis 里残留了部分样本，下次查询出来的统计均值不就彻底错了？）
- **过期策略的具体逻辑：** 这种评估数据通常具有时效性。你如何平衡“用户反复查看结果”和“计算资源回收”？

#### 1. Key 粒度：当前不是“按 Trial 拆分”

- 现在的“评估结果拆分 Key”是按方案（Scheme）拆分，不是 10,000 Trial 一条条拆。
- 流程是：先把整棵评估树写到 `raw_result`，再按 `scheme` 拆成多个 `grouped_result:{union}:{scheme}`。
- 所以它属于“1 个原始大 Key + N 个方案级小 Key”，并不是“1 万个 Trial Key”。
- 把对象拆成：
  - A 段：大而稳定（拓扑结构）
  - B 段：高频变化（状态）
- 仍然“深拷贝”但对 A 段使用“预序列化模板字节”快速反序列化复原，对 B 段每次实时拷贝。
- 语义上仍是完整副本，性能上大幅减少重复编码成本。

#### 2. 我会如何改进写入原子性（面试高分回答）

- 方案A：版本号 + 提交标记（推荐）
  - 先写到 `grouped_result:{jobId}:tmp:*`
  - 全部写完后再写 `grouped_result:{jobId}:meta`（`status=COMMITTED`）
  - 查询端只读 `COMMITTED` 版本；无标记就当无效。
- 方案B：Lua 脚本批量写 + 最后置位
  - 单脚本内完成多 key 写入和 commit 位设置，保证脚本级原子。
- 方案C：改为单 Hash + 字段写入
  - 用 `HSET key field value` 维护同一 job 的多个片段，再用 `meta` 字段作为完成标记。

#### 3. 过期策略：当前逻辑与改进建议

- 当前默认 TTL 来自 `RedisDataUtils.set` 的 `DEFAULT_CACHE_TTL_HOURS=1h`；仿真态势缓存是单独 `10min`。
- 面试可说你会分层 TTL：
  - 热结果（用户刚算完）短 TTL（如 30-120 分钟）
  - 被反复访问则“延长 TTL”或异步固化到 DB/对象存储
  - 冷数据自动过期，回收内存
- 再补一句：TTL 不应单一固定值，应该按“结果大小、访问频率、业务时效”动态策略化。



当前实现是方案级拆分，不是 Trial 级拆分；

一致性上是可用但非强原子。生产化我会加版本化临时写入 + 提交标记，读取端只认 committed 版本；

TTL 采用冷热分层，兼顾重复查看体验和 Redis 内存回收。



### MyBatis-Plus 与 DM8 的性能坑位

面对十万级节点和边，`MyBatis-Plus` 的 `saveBatch` 或分页查询在达梦数据库（DM8）上表现如何？

- 你是否遇到过达梦的锁竞争或临时表空间溢出问题？
- 针对“条件组合查询”，你设计的索引是复合索引还是覆盖索引？在处理这种复杂的图数据元数据时，你是如何在 SQL 层面优化深度关联查询的？



“在十万级节点边场景，我不直接依赖 `saveBatch` 默认行为，而是 `insertBatch + 1000 分片`，控制 SQL 长度和事务体积，减少 DM8 锁与临时空间压力。查询侧以复合索引为主、关键接口尽量覆盖索引；复杂图关联采用分阶段查询，把深度路径计算放到应用层，结合执行计划持续调优。”



### 接口幂等与并发冲突

用户在前端狂点“开始评估”按钮，或者两个用户同时对同一个“想定”发起评估。

- 你是如何做**分布式锁**或**请求去重**的？
- 此时 Redis 里的旧结果会被覆盖还是直接报错？

#### 1. 我会如何做请求去重（首选）

- 前端每次点击生成 `requestId`（或后端按请求体哈希生成），作为幂等键。
- Redis `SET idem:{requestId} IN_PROGRESS NX EX 60`
  - 成功：允许进入计算；
  - 失败：说明重复请求，直接返回“处理中”或返回已缓存结果。
- 任务完成后把结果写到 `result:{requestId}`，并把状态改 `DONE`（带 TTL）。

#### 2. 我会如何做“同想定并发互斥”（可选锁）

- 锁粒度按业务键：`lock:evaluate:{scenarioId}:{netTime}:{indicatorId}`。
- 用 Redis 分布式锁（`SET NX PX` + value token + Lua 释放）。





## 黑马点评

MybatisPlus的拦截器

```java
@Configuration
public class MybatisConfig {
    @Bean
    public MybatisPlusInterceptor mybatisPlusInterceptor() {
        MybatisPlusInterceptor interceptor = new MybatisPlusInterceptor();
        // 分页拦截器，自动实现数据库分页查询，无需手动写 LIMIT。
        interceptor.addInnerInterceptor(new PaginationInnerInterceptor(DbType.MYSQL));
        // 乐观锁拦截器，防止并发更新时的数据覆盖问题。
        interceptor.addInnerInterceptor(new OptimisticLockerInnerInterceptor());
        return interceptor;
    }
}
```



### Gemini的狠狠拷打

#### Q1：一人一单的数据结构与 BigKey 隐患

你在 Lua 脚本中，具体是用什么 Redis 数据结构来记录“当前用户已经下过单”的？如果是 `Set`，当某款爆款藏品有 100 万人参与抢购（哪怕只有 1 万个库存，但有 100 万人点击请求记录），这个 Key 会不会膨胀成 BigKey，导致单线程的 Redis 在执行 Lua 脚本时发生毫秒级甚至秒级的阻塞？如果是，你怎么解决？

#### A1:

现有语义下，不会因为「点击量」线性膨胀到 100 万：

- 只有 库存够且该用户还不在 Set 里 时才会执行 `SADD`。
- 库存只有 1 万时，最多成功 `SADD` 约 1 万次（每人最多成功一次），Set 里成员数大致 ≤ 成功下单人数 ≤ 库存上限，和「100 万次无效点击」没有一一对应关系。

所以：爆款 100 万人点、库存 1 万，这个 Set 的规模量级是「万级」成员，不是百万级。

若业务改成「每个请求都往 Set 里记一次」或「记所有 UV」，那才会按参与人数膨胀，BigKey 风险才大——那是另一种设计。

仍要注意的边界：

- 库存本身很大（例如百万级）时，Set 理论上可到百万级成员，单 Key 体积和 `SISMEMBER` 成本会上去，那时才有 BigKey / 热点 Key 问题。
- userId 很长（字符串）时，同一成员数下内存比纯数字 id 更大。

**若仍担心 BigKey / 单线程 Lua 阻塞，可以这么改**

1. 保持「Set 只记成功占位」（你现在的思路）
   成员数有业务上界（与成功量、库存挂钩），一般比「全站 UV」可控。
2. 布隆过滤器 / 布谷鸟过滤器 + DB 最终一致
   Redis 里只做「可能下过单」的快速过滤，精确一人一单仍以 DB 唯一索引为准；有误判就要容忍少量冲突在 DB 层拒绝（需产品设计）。



#### Q2：资格释放与状态回滚的“黑盒”

 你刚才提到，你的秒杀系统里有“SpringTask 定时任务实现未支付订单的到期自动关闭”。 既然抢购成功时，你在 Redis 的 `Set` 里记录了这 10,000 个用户的 `userId`，并在 Redis 里扣了库存。假设有 20% 的人（2000人）属于“黄牛恶意占座”或者“忘了支付”，15 分钟后触发了自动关单。

- **问题来了：订单关闭后，库存需要回滚吗？如果需要回滚，你该如何安全地把这 2000 个用户的 `userId` 从那个 Redis `Set` 中剔除，并加回 Redis 的库存？**
- **一致性痛点：** 关单是在 Java 的定时任务里执行的，而 Redis 状态是用 Lua 脚本控制的。你如何保证“DB 关单恢复库存”与“Redis 释放用户购买资格”这两者的**强一致性**？如果 DB 关单成功，但 Redis 里的 `userId` 没删掉，导致这批人重试依然被拦截，库存永远卖不出去（少卖死锁），你当时是怎么解决的？

#### A2：

当前项目在 DB 关单提交成功之后，用 `compensateSeckillStock` 做与 Lua 相反 的操作（见下文引用）：

- MySQL：`tb_seckill_voucher` 上 `stock = stock + 1`
- Redis 库存：`INCR seckill:stock:{voucherId}`（与 Lua 里 `INCRBY -1` 对仗）
- Redis 资格 Set：`SREM seckill:order:{voucherId} userId`（与 Lua 里 `SADD` 对仗）

```java
    /**
     * 秒杀订单关单后回补 MySQL 库存与 Redis（与 seckill.lua 语义对齐）；非秒杀券无秒杀表记录则跳过。
     */
    private void compensateSeckillStock(Long voucherId, Long userId) {
        if (seckillVoucherService.getById(voucherId) == null) {
            return;
        }
        seckillVoucherService.update()
                .setSql("stock = stock + 1")
                .eq("voucher_id", voucherId)
                .update();
        stringRedisTemplate.opsForValue().increment(RedisConstants.SECKILL_STOCK_KEY + voucherId);
        stringRedisTemplate.opsForSet().remove(RedisConstants.SECKILL_ORDER_KEY + voucherId, userId.toString());
    }
```

2000 人关单 = 2000 笔订单各自走一遍「关单成功 → 回补」；不是一次 Lua 里批量删 2000 个 member，而是 按订单、按用户 各调一次（定时任务里是循环 + 每笔事务 `afterCommit` 调回补）。

`SREM` 本身是幂等的（删两次和删一次一样）；`INCR` 若对同一笔关单执行两次会多回血，因此依赖 订单只能从「未支付」关一次（乐观锁 + 状态）避免重复关单重复回补。

实现顺序是：先在事务里把订单改成已取消并 `commit`，再在 `afterCommit` 里调 Java 依次更新 MySQL 秒杀库存 + 改 Redis



#### Q3：布隆过滤器的“业务灾难”：

针对真正的海量库存（比如发放 100 万个免费纪念数字徽章），你提出了使用**布隆过滤器（Bloom Filter）\**的降级方案。 作为技术人，你应该知道布隆过滤器存在\**假阳性（False Positive）**。也就是说，它可能会误判一个**从未下过单**的正常用户“已经下过单”，从而在 Lua 脚本阶段就无情地拒绝他的请求。

- **问题来了：在 C 端电商/数藏场景中，直接“吞掉”真实用户的购买资格会引发极高的客诉率（用户会投诉你们系统有黑幕、暗箱操作）。**
- 你刚才说“有误判就要容忍少量冲突在 DB 层拒绝”——这句逻辑是反的。布隆过滤器的误判是把“没下过单”认成“下过单”，它连 DB 层都到不了，直接在 Redis 就被拦截了。**面对这种必然发生的误拦截客诉，你在架构设计时，准备怎么向产品经理和客服团队交待？有没有技术层面的兜底或补偿方案？**

#### A3：

**正确用法——BF 只做「快速放行」，不做「快速枪毙」（推荐）**

语义要接在 「从未插入过该 user」 的 BF 上：

- BF 说「一定没下过单」（不存在 / not might contain，视实现而定）：可以走 继续抢资格 的路径（经典 BF：若判断为不在集合，则一定不在——无假阴性）。
- BF 说「可能下过单」：不能拒绝；必须再查 权威结构（Redis `SISMEMBER`、或 DB 唯一键、或带 TTL 的精确 Set）。

这样 假阳性只会多一次查表/Lua 分支，不会多一次误拒；客诉意义上的「被 BF 吞掉」在架构上就不成立。

很多「灾难」来自 把 BF 用反了：把「可能已存在」当成「一定已存在」直接拒单。



#### Q4：致命的“掉单”问题（消息发送失败）

 这是高并发秒杀中最经典的故障场景：你的 Lua 脚本在 Redis 里执行成功了（库存减了，用户的购买资格也记录了），紧接着你的 Java 代码准备把订单信息发给 Kafka。**就在这一瞬间，应用所在的 Pod/容器 突然 OOM 崩溃了，或者网络出现闪断导致 Kafka 发送失败（`send` 抛出异常或一直超时）。** 这时候，用户的资格已经被耗尽了，前端提示用户“排队中”，但实际上 Kafka 里根本没有消息，MySQL 里也永远不会有订单。这在业务上叫“吞单”。**你是如何保证 Redis 状态更新与 Kafka 消息发送的“原子性/最终一致性”的？**

#### A4：

“在极端高并发的秒杀场景下，我没有选择‘本地消息表’这种强依赖数据库事务的方案，因为那会抵消掉 MQ 削峰的作用。我采用的是**‘异步对账与延迟补偿’**的机制，来确保系统在 Pod 崩溃或网络闪断后的数据一致性。”

「主链路用 Redis Lua 扛并发；投递与落库解耦用 Kafka；承认中间会失败。用 pending/Stream 留凭证，定时对账 + 大于 P99 的延迟窗口 识别异常，幂等补偿（补 MQ 或调建单）收敛状态；唯一键 + 消费者幂等 防双单；DLQ 与监控 兜住长尾。整体是 最终一致，用工程手段把概率事件变成可修复、可度量。」



#### Q5：Kafka 消费端的重复与乱序（幂等性）

假设消息成功发到了 Kafka。由于网络重传或 Consumer 端发生 Rebalance，导致同一条下单消息被消费了两次。 在消费者对应的 Java 代码中，你具体是如何保证写入数据库时的**强幂等性**的？仅仅靠数据库的唯一索引吗？如果在插入订单前，还需要扣减用户余额或积分，你这套逻辑还安全吗？

#### A5：

`SeckillOrderConsumer` 没有按 `orderId`/offset 做去重表或幂等键处理，只是每次调用 `createVoucherOrder`。幂等主要靠 下游 `createVoucherOrder` + 数据库约束。

```java
        try {
            // 5.1.查询订单
            int count = query().eq("user_id", userId).eq("voucher_id", voucherId).count();
            // 5.2.判断是否存在
            if (count > 0) {
                // 用户已经购买过了
                log.error("不允许重复下单！");
                return;
            }

            // 6.扣减库存
            boolean success = seckillVoucherService.update()
                    .setSql("stock = stock - 1") // set stock = stock - 1
                    .eq("voucher_id", voucherId).gt("stock", 0) // where id = ? and stock > 0
                    .update();
            if (!success) {
                // 扣减失败
                log.error("库存不足！");
                return;
            }

            // 7.创建订单
            save(voucherOrder);
```

要点：

1. 按 `(user_id, voucher_id)` 先查是否已有订单：第二次消费同一业务单时，通常 在这里就 return，后面的 `stock - 1` 和 `save` 都不会再执行，因此不只是「靠唯一索引」，应用层先挡了一层，避免重复扣减秒杀库存、重复插入（在单线程顺序消费或锁串行下很有效）。
2. 用户维度 Redisson 锁 `lock:order:{userId}`：同一用户对同一券的并发消费会串行化，降低并发下「两次都看到 count=0」的概率。
3. 数据库唯一索引：项目里有 `uk_user_voucher (user_id, voucher_id)`，作为 最后一道保险（极端并发下若两道检查都漏了，插入会失败）。消息里还带固定 `orderId` 作为主键，同一消息重复插入同一主键也会失败。

所以：不是「仅仅」唯一索引，而是 「先查重 + 用户锁 + 条件更新库存 + 唯一约束/主键」 组合；但消费者本身 没有 Kafka 层面的幂等键登记。



#### Q6：先查后写的并发幻读（Phantom Read）

你提到先 `count()` 判断是否存在，如果 `count == 0` 再执行扣减和插入。

- **拷问：** 在高并发消费场景下（比如 Kafka 的多个 Partition 被多个 Consumer 实例同时消费），如果同一个用户的两条重复消息在极短的时间内被两个线程同时处理。线程 A 执行完 `count()` 发现是 0，还没来得及 `save`；此时线程 B 也执行完 `count()`，发现也是 0。**结果两个线程都进入了扣减库存和创建订单的逻辑。**
- 虽然你有“数据库唯一索引”保底，但这意味着**线程 B 的事务会抛出 `DuplicateKeyException` 并回滚**。这在高并发下会产生大量的异常堆栈和数据库回滚开销。既然你用了 **Redisson 锁**，你是如何平衡“锁的性能损耗”与“数据库异常回滚成本”的？如果 Redis 挂了导致锁失效，你的逻辑还能保证**账户余额扣减**的安全性吗？

#### A6：

同一用户、同一券、同一类重复消费时，代码里用的是 `lock:order:{userId}` 的 Redisson 锁：

- 拿到锁的线程才能执行 `count → 扣库存 → save`。
- 另一线程若在同一用户上并发进来，当前实现是 `tryLock()` 不等待：拿不到锁就直接 `return`（日志里写「不允许重复下单」），不会和持锁线程一起进「都 count=0」的那段临界区。

因此：在锁正常、且重复消息都落在「同一 userId」这条锁链上时，不会出现你描述的「A、B 都 count=0 再双写」的经典幻读窗口；第二段逻辑要么等锁后看到 count>0 直接返回，要么 tryLock 失败直接退出。

> 若重复消息被设计成 不同 userId（一般不会是同一次秒杀重复），那本来就不是「同人重复」，锁粒度也不同。

「tryLock 不等待」：这是 避免长时间占着分区消费线程；代价是 拿不到锁的那次消费可能算「未成功处理」，要配合 Kafka 重试 / 幂等消费位点策略，否则有「假失败」风险——这是 性能 vs 语义 的显式取舍，可改成 `tryLock(wait, lease)` 或 短等待 + 有界队列。

「同人重复消费用 user 维度 Redisson 锁 串行化，避免双线程都过 `count`；唯一索引兜最后一层，控制异常回滚在少数路径。锁用 `tryLock` 无等待 是换 吞吐与消费延迟，可用 带等待/重试策略 调优。Redis 挂 时锁失效不应继续裸奔写资金；要么 失败重试，要么降级 DB 锁，余额与订单同事务 + 流水幂等 才能保证资金安全。」



#### Q7：涉及“资金/积分”扣减时的致命顺序

你刚才的代码逻辑顺序是：**1.查重 -> 2.扣减库存 -> 3.创建订单**。

- **拷问：** 如果业务要求在下单时必须同步**扣减用户余额或积分**（这涉及外部资金表或服务），你打算把扣款逻辑放在哪一步？
  - 如果放在 `save(order)` 之前，扣款成功但订单创建失败了怎么办？
  - 如果放在 `save(order)` 之后，订单创建成功但扣款失败了（比如余额不足，或者网络超时）又该如何处理？
- **追问：** 这种“涉及钱”的幂等，仅仅靠 `count()` 已经不够了。你是否了解**“状态机幂等”**或**“TCC（Try-Confirm-Cancel）”**？在你的秒杀架构中，如何确保“钱不白扣，货不白送”？



**方案一：本地事务 + 状态机 + 补偿（最常见）**

流程设计：

1. 下单阶段（本地事务）
   - 创建订单（状态 = `PENDING_PAY`）
   - 扣减库存（或预扣库存）
2. 异步 or 同步调用资金系统
   - 扣款
3. 根据结果：
   - 成功 → 更新订单为 `PAID`
   - 失败 → 更新订单为 `CANCELED`，回滚库存

这里的关键是：

- **订单一定先落库（作为“事实”存在）**
- 后续操作都是围绕订单状态进行

**幂等怎么保证？**

例如扣款接口：

- 你必须传一个 **业务唯一ID（orderId）**
- 资金系统要保证：
  - 同一个 orderId 扣款只执行一次（幂等）

**方案二：TCC（Try-Confirm-Cancel）模式**

这是比“状态机补偿”更严格的一种方案，适用于“钱不能错一分钱”的场景。

三个阶段：

1. Try（预留资源）
   - 冻结余额（不是扣）
   - 预扣库存
2. Confirm（确认提交）
   - 真正扣钱
   - 真正扣库存
3. Cancel（回滚）
   - 解冻余额
   - 释放库存

秒杀场景下：

- Try：
  - 冻结用户余额
  - 预占库存（Redis / DB）
- Confirm：
  - 创建订单
  - 扣减余额
  - 扣减库存
- Cancel：
  - 释放库存
  - 解冻余额

### 为什么要使用多级缓存呢？

1. 多级缓存：为 延迟、命中率、保护 DB/Redis 分层，不是层数越多越好，要按 陈旧度容忍度 选型。
2. Caffeine 在分布式里：不追求全局强一致，用 短 TTL、写后失效/消息广播、敏感数据不进 L1、版本号回源 做到 最终一致；钱、库存 等以 Redis/DB + 协议 为准，本地缓存只做 非关键加速。



### Redisson实现一人一单的key是如何设计的？

设计含义

1. 粒度：按 `userId`，不是 `userId + voucherId`。
   因此同一用户在不同券上的 `createVoucherOrder` 会抢同一把锁，在持锁期间 串行执行（避免同一用户并发两条消费线程都通过 `count` 等逻辑）。
2. 前缀 `lock:order:`：业务前缀，便于在 Redis 里区分其它锁（如项目里还有 `lock:shop:` 一类常量风格）。
3. 与「一人一单」的关系：应用层还用 `user_id + voucher_id` 查重 + DB 唯一索引 保证 同一券同人一单；Redisson 这把锁主要解决 同人并发写 时的竞态，不是「每券一把锁」的细粒度设计。

若面试问「若希望不同券并行、只锁同人同券」，一般会改成 `lock:order:{userId}:{voucherId}` 之类；当前代码 未 这样做。



### lua的保证原子性的原理是什么？

**单线程执行命令**

Redis 主线程（处理命令的那条线程）在任一时刻 只执行一条客户端命令（或一整段 Lua）。

对其它客户端来说：你的脚本跑完之前，中间不会插入别的 `GET`/`SET`，因此不会出现「脚本执行到一半被别人改数据」这种交错。

**脚本作为「一整段」执行**

`EVAL` / `EVALSHA` 会把脚本里的 `redis.call(...)` 全部顺序执行完，在这段期间 同一条连接上的这段脚本被当作一个原子单元 提交给引擎处理。

因此脚本里多次读写（扣库存、`SADD`、`XADD` 等）在 对外可见性 上是：要么都生效完再被其它命令看到，要么出错按 Redis 规则处理（中途错误会中断后续 `redis.call`，已执行的写是否回滚取决于 Redis 版本与错误类型；常见教学表述是「整段原子」，工程上要区分 语法/运行时错误 与 显式事务）。



### 互斥锁和逻辑过期应对缓存击穿有什么不同？

缓存击穿 = 热点 key 在某一时刻失效，大量请求直接打到 DB

1）互斥锁（Mutex Lock）

**核心思路：**只允许一个线程去查数据库并重建缓存，其它线程等待

流程：

```
1. 查询缓存 → miss
2. 尝试加锁（如 Redis SETNX）
   - 成功 → 查 DB → 写缓存 → 释放锁
   - 失败 → sleep/重试 → 再查缓存
```

特点：

- ✔ 保证**强一致性**
- ✔ 不会返回旧数据
- ❌ 请求会**阻塞（等待锁）**
- ❌ 高并发下可能有**线程堆积**



2）逻辑过期（Logical Expire）

**核心思路：**缓存永不过期，但“数据内部带一个过期时间”，过期后异步重建

| 维度       | 互斥锁 | 逻辑过期               |
| ---------- | ------ | ---------------------- |
| 是否阻塞   | 会     | 不会                   |
| 数据一致性 | 强一致 | 最终一致               |
| 用户体验   | 可能慢 | 快（但可能旧）         |
| DB压力     | 低     | 更低（几乎无瞬时冲击） |
| 复杂度     | 低     | 高                     |

### 说说逻辑过期和物理过期的区别？

| 维度         | 物理过期（TTL）                                              | 逻辑过期                                                     |
| :----------- | :----------------------------------------------------------- | :----------------------------------------------------------- |
| 谁管过期     | Redis 到点删 key 或拒绝当作有效（视策略），`GET` 可能直接 nil。 | 业务/应用在反序列化后的对象里读 expireTime / version，自己判断「算不算过期」。 |
| key 是否还在 | 过期后 通常不存在（或不可见），缓存 穿透式 miss。            | key 仍可存在，读总能拿到一份 结构体/JSON。                   |
| 典型目的     | 省内存、自动淘汰、简单。                                     | 避免热点 key 同时失效；平滑更新；配合异步刷新。              |
| 风险         | 击穿、雪崩要额外设计（锁、随机 TTL、永不过期+逻辑过期等）。  | 要防止 逻辑过期后永远不刷新（异步任务失败）、旧数据暴露过久（需监控与兜底回源）。 |

一句话：物理过期是 Redis 删 key；逻辑过期是 「数据还在，但业务认为该更新了」，是否更新由应用策略决定。



### 布隆过滤器是如何防止缓存穿透的，他的假阳性是什么，怎么降低误判率？

在访问缓存/数据库之前，先判断“这个 key 是否可能存在”

访问流程（标准做法）

```
请求 → 布隆过滤器 → Redis → DB
```

具体逻辑：

1. 请求 key（如 userId=99999）
2. 先查布隆过滤器：
   - **不存在（一定不存在） → 直接返回**
   - **存在（可能存在） → 继续查缓存**
3. Redis miss → 查 DB → 回写缓存



n = 元素数量，m = 位数组大小，k = 哈希函数个数

在固定 n 下，增大 m、合理选择 k，可以降低误判率

布隆过滤器 **不能删除元素**
